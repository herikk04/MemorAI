# MemorAI backend dependencies — pinned for reproducibility
# Sprint 0: foundation. IA/analytics deps added in Sprint 2+.

Django==5.2.5
djangorestframework==3.16.1

# Config & secrets via env
django-environ==0.11.2

# Database (dev uses SQLite; prod uses PostgreSQL+pgvector)
psycopg[binary]==3.2.1

# Auth
djangorestframework-simplejwt==5.3.1

# API docs
drf-spectacular==0.27.2

# CORS (frontend <-> backend)
django-cors-headers==4.4.0

# Async jobs (IA analytics) — used from Sprint 4
celery==5.4.0
redis==5.0.8

# LLM clients (IA core) — used from Sprint 2
openai==1.40.6
anthropic==0.34.2

# Vector store (pgvector) — used from Sprint 3
pgvector==0.3.5

# Observability
structlog==24.4.0

# Dev / test
pytest==8.3.2
pytest-django==4.8.0
ruff==0.6.3
mypy==1.11.2
